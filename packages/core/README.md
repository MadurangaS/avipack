# @avipack/core

Core utilities for Avipack.

This package contains the project brain creation/checking logic, template registry, bot manifest types, and the bundled MVP `generic-brain-only` template used by `avipack init`.
