testing:
  strategy: "Node.js built-in test runner for CLI and core package validation."
  test_cases:
    - id: TEST-001
      title: "Brain initialization creates required files"
      type: unit
      related_requirements:
        - REQ-001
      command: "pnpm test"

    - id: TEST-002
      title: "Requirement trace validation detects unknown references"
      type: unit
      related_requirements:
        - REQ-002
      command: "pnpm test"

    - id: TEST-003
      title: "Bot commands remain manual and owner-controlled"
      type: integration
      related_requirements:
        - REQ-003
      command: "pnpm test"
